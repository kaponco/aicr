# {{ .Name }}

{{ if .IsOLM -}}
Namespace: {{ .Namespace }}

**Type:** OLM (Operator Lifecycle Manager)

{{ if .HasInstallFiles -}}
**Prerequisites:** This component requires an OLM-enabled cluster (OpenShift or Kubernetes with OLM installed).

## Step 1: Deploy OLM Resources

```bash
# Create namespace
kubectl create namespace {{ .Namespace }} --dry-run=client -o yaml | kubectl apply -f -

# Apply OLM installation manifests (Subscription, OperatorGroup, etc.)
{{ range .InstallFiles -}}
kubectl apply -f {{ .Path }}
{{ end -}}
```

## Step 2: Wait for Operator Ready

```bash
# Wait for operator to be ready
kubectl wait --for=jsonpath='{.status.phase}'=Succeeded csv \
  -n {{ .Namespace }} \
  --all \
  --timeout=5m
```

## Step 3: Deploy Custom Resources

```bash
# Apply custom resources
kubectl apply -f resources.yaml -n {{ .Namespace }}
```
{{ else -}}
{{ if ne (len .CustomResources) 0 -}}
**Prerequisites:** This component's operator must be pre-installed in the cluster.

## Deploy Custom Resources

```bash
# Create namespace
kubectl create namespace {{ .Namespace }} --dry-run=client -o yaml | kubectl apply -f -

# Apply custom resources
kubectl apply -f resources.yaml -n {{ .Namespace }}
```
{{ end -}}
{{ end -}}

## Verify

```bash
{{ if .HasInstallFiles -}}
# Check operator installation
kubectl get csv -n {{ .Namespace }}

{{ end -}}
{{ if ne (len .CustomResources) 0 -}}
# Check custom resources
kubectl get -f resources.yaml -n {{ .Namespace }}
{{ end -}}
```

## Undeploy

```bash
{{ if ne (len .CustomResources) 0 -}}
# Delete custom resources first
kubectl delete -f resources.yaml -n {{ .Namespace }} --ignore-not-found

{{ end -}}
{{ if .HasInstallFiles -}}
# Delete OLM resources
{{ range .InstallFiles -}}
kubectl delete -f {{ .Path }} --ignore-not-found
{{ end -}}
{{ end -}}
```
{{ else if .IsKustomize -}}
Source: {{ .Repository }}
Path: {{ .Path }}
{{ if .Tag -}}Tag: {{ .Tag }}{{ end }}
Namespace: {{ .Namespace }}

## Install

```bash
kubectl create namespace {{ .Namespace }} --dry-run=client -o yaml | kubectl apply -f -
kustomize build '{{ .Repository }}//{{ .Path }}{{ if .Tag }}?ref={{ .Tag }}{{ end }}' \
  | kubectl apply -n {{ .Namespace }} -f -
```
{{ if .HasManifests }}
After the kustomization is applied, apply additional manifests:

```bash
kubectl apply -f manifests/
```
{{ end }}
{{ if and (ne .Service "ocp") (not .IsOLM) }}
## Upgrade

```bash
kustomize build '{{ .Repository }}//{{ .Path }}{{ if .Tag }}?ref={{ .Tag }}{{ end }}' \
  | kubectl apply -n {{ .Namespace }} -f -
```
{{ end }}
## Uninstall

```bash
kustomize build '{{ .Repository }}//{{ .Path }}{{ if .Tag }}?ref={{ .Tag }}{{ end }}' \
  | kubectl delete -n {{ .Namespace }} --ignore-not-found -f -
```
{{ else -}}
Chart: {{ .Repository }}/{{ .ChartName }}
Version: {{ .Version }}
Namespace: {{ .Namespace }}

## Install

```bash
{{ if .IsOCI -}}
helm upgrade --install {{ .Name }} {{ .Repository }}/{{ .ChartName }} \
  --version {{ .Version }} \
  -n {{ .Namespace }} --create-namespace \
  -f values.yaml \
  -f cluster-values.yaml \
  --wait --timeout 10m
{{ else -}}
helm upgrade --install {{ .Name }} {{ .ChartName }} \
  --repo {{ .Repository }} \
  --version {{ .Version }} \
  -n {{ .Namespace }} --create-namespace \
  -f values.yaml \
  -f cluster-values.yaml \
  --wait --timeout 10m
{{ end -}}
```
{{ if .HasManifests }}
After the chart is installed, apply additional manifests:

```bash
kubectl apply -f manifests/
```
{{ end }}
{{ if and (ne .Service "ocp") (not .IsOLM) }}
## Upgrade

```bash
{{ if .IsOCI -}}
helm upgrade {{ .Name }} {{ .Repository }}/{{ .ChartName }} \
  --version {{ .Version }} \
  -n {{ .Namespace }} \
  -f values.yaml \
  -f cluster-values.yaml \
  --wait --timeout 10m
{{ else -}}
helm upgrade {{ .Name }} {{ .ChartName }} \
  --repo {{ .Repository }} \
  --version {{ .Version }} \
  -n {{ .Namespace }} \
  -f values.yaml \
  -f cluster-values.yaml \
  --wait --timeout 10m
{{ end -}}
```
{{ end }}
## Uninstall

```bash
helm uninstall {{ .Name }} -n {{ .Namespace }}
```
{{ end -}}
